# 抽象解释的过去现在与未来

> Abstract Interpretion : Past, Present and Future

## 1.抽象

能不抽象吗

## 2.作用域

任何基于抽象解释的工具都会在成千上万的反例上被打倒，因为这一技术的应用领域通常要面对的就是一些不可判定(undecidable)的问题.

## 3.静态分析

静态分析是抽象解释的起源地，在静态分析中，可达的状态会被表示为本地区间数值不变量(local interval numerical invariant)，这通常被理解为类型推导的一种泛化。